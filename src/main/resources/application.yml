spring:
  application:
    name: Coding Challenge Circus
  datasource:
    url: jdbc:h2:mem:quotesdb
    driverClassName: org.h2.Driver
    username: sa
    password: password
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: none
    generate-ddl: false
  h2:
    console:
      enabled: true
server:
  port: 8090
